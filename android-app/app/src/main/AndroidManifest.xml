<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/barangay_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/barangay_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BarangayApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.AnnouncementsActivity" />
        <activity android:name=".ui.ComplaintActivity" />
        <activity android:name=".ui.RequestDocumentActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
